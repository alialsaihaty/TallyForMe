# Use the README file as a reference to customize settings.

Client:
  fields:
    default: company_id, first_name, last_name
    form: company_id, first_name, last_name
  relationships: addresses, phones, emails
  application: Application
