# Use the README file as a reference to customize settings.

Service:
  fields:
    default: name, unit, tax1, tax2, description, cost, company_id
    form: name, unit, tax1, tax2, description, cost, company_id
  relationships: invoiced_services, invoices
  application: Application
